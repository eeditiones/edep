/**
 * @type {import('prettier').Config}
 */
const config = {
    singleQuote: true,
    arrowParens: 'avoid',
    tabWidth: 4,
    printWidth: 120,
    bracketSameLine: true,
};

export default config;
